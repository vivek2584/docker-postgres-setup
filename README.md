# postgres setup file for docker
- run the container :-
```zsh

docker-compose up -d
```
- stop :-
```zsh
docker-compose down
```
- to access the db via terminal :-


```zsh
docker-compose exec database /bin/bash
```
